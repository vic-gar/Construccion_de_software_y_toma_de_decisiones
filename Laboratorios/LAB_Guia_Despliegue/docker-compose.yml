services:
  mi-servidor:
    build: .
    container_name: mi-servidor
    user: estudiante
    working_dir: /home/estudiante
    ports:
      - "80:80"
      - "3000:3000"
    tty: true
    stdin_open: true